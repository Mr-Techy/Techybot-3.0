const db = require('quick.db');

module.exports = {
	name: 'start',
	description: 'Starts a user\'s profile.',
	aliases: ['makeprofile'],
	usage: '[business name]',
	run: async (bot, message, args) => {
		
	}
}